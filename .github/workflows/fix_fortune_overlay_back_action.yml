name: fix-fortune-overlay-back-action

on:
  push:
    branches:
      - implementation/ripple-surface-input
    paths:
      - .github/workflows/fix_fortune_overlay_back_action.yml

permissions:
  contents: write

jobs:
  patch-and-verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: implementation/ripple-surface-input
          fetch-depth: 0
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
      - name: Apply fortune/back patch
        run: python3 tooling/fix_fortune_overlay_back_action.py
      - name: Resolve dependencies
        run: flutter pub get
      - name: Analyze
        run: flutter analyze
      - name: Test
        run: flutter test
      - name: Commit verified change
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add lib/cha_time_app.dart
          git commit -m "Make fortune card translucent and calendar action quiet"
          git push origin HEAD:implementation/ripple-surface-input
